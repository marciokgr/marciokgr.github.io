import { AppContent, Lang } from '../types';
import { en } from './en';
import { es } from './es';
import { pt } from './pt';

export const translations: Record<Lang, AppContent> = {
  pt,
  en,
  es,
};
